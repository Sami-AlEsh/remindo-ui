# Where the Remindo API lives. The dev server proxies /api
# requests here so the UI stays same-origin with the API —
# required, because the refresh token is a sameSite=strict cookie.
VITE_API_TARGET=http://localhost:3000
